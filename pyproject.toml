[project]
name = "mechatronics-sensor-calibration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "seaborn",
    "matplotlib",
    "numpy",
    "scikit-learn>=1.7.2",
]
